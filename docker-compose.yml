version: '3.9'

services:
  budgetbaby:
    build: .
    container_name: budgetbaby
    ports:
      - "8080:8080"
    env_file:
      - secrets/env-file
    volumes:
      - ./secrets:/app/secrets
    restart: unless-stopped
