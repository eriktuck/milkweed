version: '3.9'

services:
  budgetbaby:
    build: .
    container_name: budgetbaby
    ports:
      - "8080:8080"
    environment:
      - ENV_PATH=/app/secrets/env-file
    volumes:
      - ./secrets:/app/secrets
    restart: unless-stopped
